# MahdiArts.ir - وب‌سایت شخصی مهدی باغبان‌پور

وب‌سایت شخصی و حرفه‌ای برای ارائه خدمات طراحی و توسعه وب‌سایت و اپلیکیشن‌های موبایل

## 📋 اطلاعات پروژه

- **نام:** MahdiArts.ir
- **مالک:** مهدی باغبان‌پور (Mahdi Baghebanpour)
- **تخصص:** مهندس ارشد کامپیوتر با ۱۰ سال تجربه
- **دامنه:** https://mahdiarts.ir
- **ایمیل:** info@mahdiarts.ir
- **واتساپ:** +989306880801
- **اینستاگرام:** [Mehdi.Bagheban](https://instagram.com/Mehdi.Bagheban)

## 🚀 ویژگی‌های اصلی

### 1. سیستم چندزبانه (i18n)
- پشتیبانی از 10 زبان: فارسی، انگلیسی، عربی، ترکی، آلمانی، فرانسوی، اسپانیایی، روسی، چینی، ایتالیایی
- منوی dropdown زیبا برای انتخاب زبان
- ذخیره انتخاب زبان در localStorage
- پشتیبانی کامل از RTL برای زبان‌های فارسی، عربی و عبری
- استفاده از فونت Vazirmatn برای فارسی

### 2. بخش‌های سایت
- **Hero Section:** معرفی شخصی و حرفه‌ای
- **About Section:** درباره من و تجربیات
- **Services Section:** 6 سرویس اصلی
- **Pricing Section:** 3 پلن قیمت‌گذاری (پایه، حرفه‌ای، سازمانی)
- **Contact Section:** فرم تماس پیشرفته با آپلود فایل

### 3. فرم تماس پیشرفته
- فیلدهای کامل: نام، ایمیل، واتساپ، انتخاب پلن، پیام، آپلود فایل
- اعتبارسنجی در سمت کلاینت و سرور
- یکپارچه‌سازی با Cloudflare Workers
- ارسال ایمیل خودکار به کاربر و مدیر
- پشتیبانی از آپلود فایل (تصاویر و PDF، حداکثر 5MB)

### 4. بهینه‌سازی SEO
- Meta tags کامل
- Open Graph tags
- Twitter Card
- Schema.org structured data
- Sitemap.xml
- Robots.txt
- hreflang tags برای چندزبانه

## 📁 ساختار پروژه

```
MahdiArts.ir/
├── assets/
│   ├── css/
│   │   └── main.css
│   ├── fonts/
│   │   └── Vazirmatn-*.woff2
│   ├── i18n/
│   │   ├── fa.json
│   │   ├── en.json
│   │   ├── ar.json
│   │   ├── tr.json
│   │   └── ... (سایر زبان‌ها)
│   ├── img/
│   │   ├── logo/
│   │   ├── about/
│   │   └── ...
│   └── js/
│       ├── main.js
│       ├── i18n.js
│       └── contact-form.js
├── workers/
│   └── contact-form.js
├── index.html
├── sitemap.xml
├── robots.txt
├── wrangler.toml
└── README.md
```

## 🛠️ نصب و راه‌اندازی

### پیش‌نیازها
- Node.js (برای Cloudflare Workers)
- Git
- حساب Cloudflare (برای Workers)

### مراحل نصب

1. **کلون کردن پروژه:**
```bash
git clone https://github.com/yourusername/mahdiarts.ir.git
cd mahdiarts.ir
```

2. **نصب وابستگی‌ها:**
```bash
npm install -g wrangler
```

3. **تنظیم Cloudflare Worker:**
   - فایل `wrangler.toml` را ویرایش کنید
   - `YOUR_ACCOUNT_ID` را با Account ID خود جایگزین کنید
   - متغیرهای محیطی را در Cloudflare Dashboard تنظیم کنید

4. **Deploy Worker:**
```bash
cd workers
wrangler publish
```

## 📦 Deploy

### GitHub Pages
1. Repository را به GitHub push کنید
2. Settings → Pages → Source را روی `main branch` تنظیم کنید
3. سایت در `https://username.github.io/mahdiarts.ir` در دسترس خواهد بود

### Cloudflare
1. دامنه `mahdiarts.ir` را به Cloudflare اضافه کنید
2. DNS records را تنظیم کنید
3. SSL/TLS را فعال کنید
4. Worker را deploy کنید

## 🔧 تنظیمات

### متغیرهای محیطی Worker
در Cloudflare Dashboard → Workers → Settings → Variables:
- `RESEND_API_KEY`: کلید API Resend (به‌صورت Secret)
- `FROM_EMAIL`: ایمیل فرستنده با دامنه تأیید‌شده Resend (مثلاً `noreply@mahdiarts.ir`)
- `ADMIN_EMAIL`: ایمیل مدیر
- `ALLOWED_ORIGINS`: دامنه‌های مجاز برای CORS

### تنظیم Resend Secrets
برای تنظیم Secretهای Resend در محیط توسعه و تولید:

```bash
wrangler secret put RESEND_API_KEY
wrangler secret put RESEND_API_KEY --env production
```

متن: برای جلوگیری از خطا و اسپم، دامنه ارسال را در Resend Dashboard تأیید کنید (DNS/DKIM) و از `FROM_EMAIL` با همان دامنه استفاده کنید.

نمونه تنظیم متغیرهای غیرسری در `wrangler.toml`:

```toml
[vars]
FROM_EMAIL = "noreply@mahdiarts.ir"
ADMIN_EMAIL = "info@mahdiarts.ir"
ALLOWED_ORIGINS = "https://mahdiarts.ir,https://www.mahdiarts.ir"
```

### Environment Variables
این جدول متغیرهای موردنیاز Worker را به‌صورت شفاف نشان می‌دهد:

| متغیر | توضیحات | مثال |
|-------|---------|------|
| `RESEND_API_KEY` | کلید API از `resend.com` | `re_xxxx` |
| `FROM_EMAIL` | ایمیل فرستنده با دامنه تأیید‌شده در Resend | `noreply@mahdiarts.ir` |
| `ADMIN_EMAIL` | ایمیل مدیر | `info@mahdiarts.ir` |
| `ALLOWED_ORIGINS` | دامنه‌های مجاز CORS | `https://mahdiarts.ir` |

یادداشت:
- `MAILGUN_DOMAIN` در این پروژه نیاز نیست و حذف شده است.

### فایل‌های ترجمه
فایل‌های ترجمه در `assets/i18n/` قرار دارند. برای افزودن زبان جدید:
1. فایل JSON جدید ایجاد کنید (مثلاً `de.json`)
2. ساختار مشابه `en.json` را دنبال کنید
3. در `assets/js/i18n.js` زبان را به لیست اضافه کنید

## 📝 TODO

- [ ] تکمیل ترجمه‌های باقی‌مانده (de, fr, es, ru, zh, it)
- [ ] پیاده‌سازی کامل Cloudflare Worker (آپلود فایل به R2، ارسال ایمیل)
- [ ] افزودن reCAPTCHA v3
- [ ] بهینه‌سازی تصاویر
- [ ] تست کامل cross-browser
- [ ] افزودن Analytics

## 📄 لایسنس

تمام حقوق و لایسنس این پروژه متعلق به MahdiArts است.

## 👤 توسعه‌دهنده

**مهدی باغبان‌پور**
- ایمیل: info@mahdiarts.ir
- واتساپ: +989306880801
- اینستاگرام: [@Mehdi.Bagheban](https://instagram.com/Mehdi.Bagheban)

---

ساخته شده با ❤️ توسط مهدی باغبان‌پور
