# تغییرات پروژه

## 2025-11-09

- اعمال گسترده فونت «وزیرمتن» (Vazirmatn) برای تمام متون فارسی سایت.
  - افزودن تعریف فونت متغیر `Vazirmatn[wght].woff2` با محدوده وزن 100–900.
  - اعمال قوانین زبان‌محور با `:lang(fa)` برای پوشش تمام عناصر متنی (`p, span, div, h1–h6, a, button, input, textarea, select, label`).
  - اطمینان از استفاده از Vazirmatn در حالت RTL و عناصر فرم.
  - تنظیم fallback به فونت‌های سیستمی در صورت عدم بارگذاری فونت.
- بهینه‌سازی عملکرد:
  - افزودن `preload` برای فونت متغیر Vazirmatn در `index.html` جهت کاهش زمان رندر.

توجه: در حال حاضر فایل‌های فونت موجود در پوشه `assets/fonts` از نوع `woff2` هستند. در صورت نیاز به فرمت‌های `woff` یا `ttf`، پس از اضافه‌کردن فایل‌ها می‌توان منابع آن‌ها را به تعریف‌های `@font-face` اضافه کرد.

