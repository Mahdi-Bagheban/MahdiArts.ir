# 🎨 راهنمای استایل (Style Guide)

این مستند استانداردهای طراحی و تایپوگرافی پروژه MahdiArts.ir را تعریف می‌کند.

## 🖋️ تایپوگرافی فارسی (Vazirmatn)

- **فونت مرجع:** `Vazirmatn`
- **مسیر فایل‌ها:** `assets/fonts/` (فرمت `woff2`)
- **نحوه تعریف:** استفاده از `@font-face` در `assets/css/main.css` با پشتیبانی از نسخه متغیر (Variable Font).

### ویژگی‌ها
- **Font Display:** `swap` برای جلوگیری از FOIT.
- **وزن‌ها:** پشتیبانی از تمام وزن‌ها (Thin تا Black) به لطف فونت متغیر.

## 🌐 تنظیمات چندزبانه (i18n)

### اعمال فونت بر اساس زبان
هنگامی که زبان فارسی (`lang="fa"`) یا جهت راست‌چین (`dir="rtl"`) فعال است:
- متغیرهای CSS زیر به `Vazirmatn` تغییر می‌کنند:
  - `--default-font`
  - `--heading-font`
  - `--nav-font`

### انتخابگرهای CSS
```css
:lang(fa) body,
:lang(ar) body {
  font-family: var(--persian-font);
}
```

## ↔️ پشتیبانی RTL/LTR

- **تشخیص خودکار:** اسکریپت `assets/js/i18n.js` جهت صفحه (`dir`) را بر اساس زبان انتخاب شده تنظیم می‌کند.
- **کلاس‌های کمکی:** کلاس‌های `rtl` و `ltr` به تگ `body` اضافه می‌شوند تا استایل‌دهی اختصاصی امکان‌پذیر باشد.

## 💡 توصیه‌های توسعه

1. **استفاده از متغیرها:** همیشه از متغیرهای CSS (`--default-font`, `--primary-color`, ...) استفاده کنید.
2. **پرهیز از Hard-coding:** هرگز نام فونت یا رنگ‌ها را مستقیماً در استایل‌های inline یا کلاس‌های اختصاصی ننویسید.
3. **پیش‌بارگذاری (Preload):** فونت‌های اصلی در `index.html` برای افزایش سرعت لود Preload شده‌اند.

## ✅ چک‌لیست کیفیت

- [ ] بررسی نمایش صحیح فونت فارسی در مرورگرهای مختلف (Chrome, Firefox, Safari).
- [ ] اطمینان از عدم وجود Layout Shift (CLS) هنگام لود فونت.
- [ ] تست خوانایی در موبایل و دسکتاپ.

---

**توسعه‌دهنده: مهدی باغبان‌پور**

